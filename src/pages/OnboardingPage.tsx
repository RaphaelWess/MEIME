/**
 * OnboardingPage — placeholder stub.
 * Real implementation in plan 02-03 (CNPJ form + BrasilAPI lookup + empresa_mei save).
 * This stub exists so /onboarding route compiles and ProtectedRoute redirect works.
 */
export default function OnboardingPage() {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <p className="text-zinc-500 text-base">Onboarding</p>
    </div>
  )
}
