---
phase: 01-fundacao-e-infraestrutura
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - src/index.css
  - src/main.tsx
  - src/App.tsx
  - src/vite-env.d.ts
  - components.json
  - src/test/setup.ts
  - vitest.config.ts
autonomous: true
requirements:
  - INFRA

must_haves:
  truths:
    - "npm run dev starts the dev server without errors"
    - "npm run build completes without TypeScript or Vite errors"
    - "npm run test -- --run passes (vitest smoke test)"
    - "Path alias @/* resolves in both runtime (Vite) and TypeScript (editor)"
    - "shadcn/ui components.json exists and is configured for Tailwind 4"
    - "Tailwind 4 CSS-first config active — no tailwind.config.js file exists"
  artifacts:
    - "package.json (all Phase 1 dependencies installed)"
    - "vite.config.ts (react + tailwindcss + VitePWA plugins + @/* alias)"
    - "tsconfig.json and tsconfig.app.json (@/* path alias in both)"
    - "src/index.css (@import 'tailwindcss' + shadcn @theme inline block)"
    - "components.json (shadcn/ui config)"
    - "src/test/setup.ts (vitest + jsdom setup)"
  key_links:
    - "vite.config.ts alias must match tsconfig paths — mismatch causes TS errors but no runtime failures"
    - "shadcn init must run AFTER path alias is set up, BEFORE any shadcn component is added"
    - "Tailwind @theme tokens added AFTER shadcn init to avoid CSS variable conflicts (Pitfall 1)"
---

<objective>
Scaffold the React+Vite+TypeScript project, install all Phase 1 npm dependencies, configure Tailwind 4 CSS-first, set up path aliases in Vite and TypeScript configs, initialize shadcn/ui, and configure Vitest with jsdom for the test infrastructure.

Purpose: This plan creates the empty but fully wired project shell. All subsequent plans add files to this scaffold. No existing codebase — this is greenfield.

Output: A working React SPA that runs locally (`npm run dev`), builds (`npm run build`), and passes a smoke test (`npm run test -- --run`). The shadcn CLI is ready to add components.

Note: Do NOT create a GitHub repo in this plan — that is covered in Plan 06 (deploy). Work in the local directory.
</objective>

<execution_context>
@C:\Users\rapha\.claude\gsd-core\workflows\execute-plan.md
@C:\Users\rapha\.claude\gsd-core\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-fundacao-e-infraestrutura/01-RESEARCH.md
@.planning/phases/01-fundacao-e-infraestrutura/01-CONTEXT.md
@.planning/phases/01-fundacao-e-infraestrutura/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold project + install all Phase 1 dependencies</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, src/vite-env.d.ts</files>
  <read_first>
    - .planning/phases/01-fundacao-e-infraestrutura/01-RESEARCH.md (section "Standard Stack" — exact package versions and install commands; section "Environment Availability" — Node version check)
    - .claude/CLAUDE.md (Technology Stack table — exact versions; "What NOT to Use" section)
  </read_first>
  <action>
    First verify Node version: `node --version` must show 20.19.x or 22.12.x or higher. If not, stop and note the version mismatch — Vite 8 will fail on older Node.

    Run the scaffold and install sequence from RESEARCH.md "Installation" section:

    Step 1 — Scaffold (run in the project working directory, NOT inside a subdirectory):
    `npm create vite@latest . -- --template react-ts`
    Accept overwrite if prompted.

    Step 2 — Install Tailwind 4 + shadcn prerequisites:
    `npm install tailwindcss@4.3.2 @tailwindcss/vite@4.3.2`
    `npm install -D @types/node`

    Step 3 — Routing + State + Server state:
    `npm install react-router@8.0.1 zustand@5.0.14 @tanstack/react-query@5.101.2`

    Step 4 — Supabase:
    `npm install @supabase/supabase-js@2.108.2`

    Step 5 — PWA:
    `npm install -D vite-plugin-pwa@1.3.0`

    Step 6 — Icons:
    `npm install lucide-react@1.22.0`

    Step 7 — Test infrastructure:
    `npm install -D @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom vitest`

    After install, update `vite.config.ts` to include:
    - Import `path` from 'path'
    - Import `tailwindcss` from '@tailwindcss/vite'
    - Import `react` from '@vitejs/plugin-react'
    - Import `{ defineConfig }` from 'vite'
    - Import `{ VitePWA }` from 'vite-plugin-pwa'
    - plugins array: [react(), tailwindcss(), VitePWA({ registerType: 'autoUpdate', manifest: { name: 'MEIME', short_name: 'MEIME', description: 'Gestão gratuita para MEI', theme_color: '#16A34A', background_color: '#FFFFFF', display: 'standalone', start_url: '/', icons: [{ src: '/icon-192.png', sizes: '192x192', type: 'image/png' }, { src: '/icon-512.png', sizes: '512x512', type: 'image/png' }] }, workbox: { globPatterns: [] } })]
    - resolve.alias: { '@': path.resolve(__dirname, './src') }
    - test block: { environment: 'jsdom', setupFiles: ['./src/test/setup.ts'], globals: true }

    Update `tsconfig.json` compilerOptions to add:
    - "baseUrl": "."
    - "paths": { "@/*": ["./src/*"] }

    Update `tsconfig.app.json` compilerOptions to add the same "baseUrl" and "paths" entries (Pitfall 2 — must be in BOTH files).

    Do NOT create `tailwind.config.js` — Tailwind 4 uses CSS-first config only.
    Do NOT add `postcss.config.js` — @tailwindcss/vite plugin handles everything.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0 (no TypeScript errors, no Vite errors)
    - `package.json` contains react@19, vite@8, tailwindcss@4, @supabase/supabase-js@2, react-router@8, zustand@5, @tanstack/react-query@5, vite-plugin-pwa, lucide-react, @testing-library/react, vitest
    - `vite.config.ts` imports and uses react(), tailwindcss(), VitePWA() plugins
    - `vite.config.ts` has resolve.alias with '@' mapped to './src'
    - `vite.config.ts` has test.environment set to 'jsdom'
    - `tsconfig.json` compilerOptions has "paths": { "@/*": ["./src/*"] }
    - `tsconfig.app.json` compilerOptions has "paths": { "@/*": ["./src/*"] } (same alias in both)
    - No `tailwind.config.js` file exists in the project root
    - No `postcss.config.js` file exists
  </acceptance_criteria>
  <done>Project scaffolded with all dependencies installed; build passes; path aliases configured in both Vite and TypeScript configs.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind 4 CSS-first + initialize shadcn/ui + set up Vitest</name>
  <files>src/index.css, components.json, src/test/setup.ts, src/App.tsx, src/main.tsx</files>
  <read_first>
    - .planning/phases/01-fundacao-e-infraestrutura/01-RESEARCH.md (section "Pattern 6: Tailwind 4 CSS-first Config"; section "Wave 0 Gaps" for test setup; "Common Pitfalls" Pitfall 1 re shadcn init order)
    - .planning/phases/01-fundacao-e-infraestrutura/01-UI-SPEC.md (Design System table — font stack; Color table — accent and destructive tokens)
  </read_first>
  <action>
    Step 1 — Replace src/index.css with Tailwind 4 CSS-first config:
    First line: `@import "tailwindcss";`
    Do NOT add custom @theme tokens yet — shadcn init will inject its @theme inline block next, and tokens must come after (Pitfall 1).
    Delete all default Vite boilerplate CSS from src/index.css.

    Step 2 — Initialize shadcn/ui (MUST run AFTER path alias is configured):
    `npx shadcn@latest init`
    When prompted:
    - Style: New York (or Default — both work with Tailwind 4)
    - Base color: Zinc
    - CSS variables: Yes
    Accept all other defaults. This creates components.json and injects @theme inline block into src/index.css.

    Step 3 — Add MEIME-specific @theme tokens to src/index.css AFTER the shadcn-generated @theme inline block:
    Add a separate `@theme { }` block containing:
    - `--color-accent: #16A34A;` (green-600 — active tab, FAB, primary CTA per UI-SPEC)
    - `--color-destructive: #DC2626;` (red-600 — delete account, errors per UI-SPEC)

    Step 4 — Install shadcn components needed for Phase 1:
    `npx shadcn@latest add button input label alert alert-dialog separator avatar`

    Step 5 — Create src/test/setup.ts:
    Import '@testing-library/jest-dom' (extends expect with DOM matchers).
    No other setup needed for Phase 1.

    Step 6 — Simplify src/App.tsx to a minimal placeholder that compiles (actual router will be added in Plan 04):
    Export a default App function that returns a div with text "MEIME scaffold" — this is temporary, replaced in Plan 04.

    Step 7 — Ensure src/main.tsx renders <App /> with React.StrictMode and imports src/index.css.

    Step 8 — Create src/test/App.test.tsx with a smoke test:
    Import render from @testing-library/react, import App.
    Test: render() of App does not throw.
    Use `screen.getByText('MEIME scaffold')` to confirm render works.
  </action>
  <verify>
    <automated>npm run test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run` exits 0 with "1 passed" (smoke test passes)
    - `src/index.css` first line is `@import "tailwindcss";`
    - `src/index.css` contains `@theme inline {` block (injected by shadcn init)
    - `src/index.css` contains a separate `@theme {` block with `--color-accent: #16A34A` and `--color-destructive: #DC2626`
    - `components.json` exists (shadcn/ui config file)
    - `src/components/ui/` directory exists with at least button.tsx (shadcn component)
    - `src/test/setup.ts` exists and imports '@testing-library/jest-dom'
    - No `tailwind.config.js` file exists
  </acceptance_criteria>
  <done>Tailwind 4 configured CSS-first; shadcn/ui initialized with all Phase 1 components added; Vitest smoke test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Package install; supply chain attack vector |
| VITE_ env vars → browser bundle | Any VITE_-prefixed variable is included in the client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-02 | Elevation of Privilege | VITE_ env var exposure | high | mitigate | Only VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY are exposed; service_role key must never be in a VITE_-prefixed var. Enforced by code review in Plan 03. |
| T-1-SC | Tampering | npm package installs | high | mitigate | Package Legitimacy Audit completed in RESEARCH.md — all packages verified on npm registry with weekly downloads and authoritative GitHub orgs; no SLOP or genuine SUS packages |
</threat_model>

<verification>
1. `npm run build` exits 0
2. `npm run test -- --run` exits 0 with smoke test passing
3. No tailwind.config.js exists (`ls tailwind.config.js 2>&1` returns "No such file")
4. components.json exists (`ls components.json`)
5. src/components/ui/ directory exists with at least one .tsx file
6. tsconfig.app.json contains "@/*" path (grep for it)
</verification>

<success_criteria>
Project scaffolded and configured. `npm run dev`, `npm run build`, and `npm run test -- --run` all pass. shadcn/ui initialized. No tailwind.config.js. Path alias @/* works in both Vite and TypeScript.
</success_criteria>

<output>
Create `.planning/phases/01-fundacao-e-infraestrutura/01-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Type | Name | Location |
|------|------|----------|
| Config | package.json | project root |
| Config | vite.config.ts | project root |
| Config | tsconfig.json | project root |
| Config | tsconfig.app.json | project root |
| Config | components.json | project root (shadcn/ui) |
| CSS | src/index.css | @import tailwindcss + @theme inline + MEIME tokens |
| Test setup | src/test/setup.ts | vitest + jsdom matchers |
| Smoke test | src/test/App.test.tsx | renders App without crashing |
| React entry | src/main.tsx | ReactDOM.createRoot + StrictMode |
| React root | src/App.tsx | placeholder (replaced in Plan 04) |
| shadcn components | src/components/ui/button.tsx | shadcn Button |
| shadcn components | src/components/ui/input.tsx | shadcn Input |
| shadcn components | src/components/ui/label.tsx | shadcn Label |
| shadcn components | src/components/ui/alert.tsx | shadcn Alert |
| shadcn components | src/components/ui/alert-dialog.tsx | shadcn AlertDialog |
| shadcn components | src/components/ui/separator.tsx | shadcn Separator |
| shadcn components | src/components/ui/avatar.tsx | shadcn Avatar |
</output>
