---
phase: 03-controle-financeiro-core
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - src/utils/categories.ts
  - src/services/transacao.service.ts
  - src/services/transacao.service.test.ts
autonomous: true
requirements:
  - FIN-01
  - FIN-02
  - FIN-05
must_haves:
  truths:
    - transacaoService.getByMonth(year, month) queries supabase.from('transacoes') with gte/lte date range and order desc
    - transacaoService.create validates cents > 0 && cents <= 999_999_999 before insert (T-02 mitigation per D-06)
    - transacaoService.create validates tipo in ['entrada', 'saida'] and tipo_pessoa in ['PF', 'PJ', null] (T-03 mitigation)
    - transacaoService.create validates data matches /^\d{4}-\d{2}-\d{2}$/ before insert (T-05 mitigation)
    - "npx vitest run src/services/transacao.service.test.ts" exits 0 (GREEN — all service tests pass)
    - CATEGORIAS_ENTRADA exports 3 entries including 'Servicos Prestados' and 'Outros'
    - CATEGORIAS_SAIDA exports 8 entries
    - Categoria type is a union of all TODAS_CATEGORIAS entries
  artifacts:
    - src/utils/categories.ts
    - src/services/transacao.service.ts
  key_links:
    - transacao.service.ts imports supabase from '@/lib/supabase' (same as empresa.service.ts)
    - Transacao interface defines valor as number (INTEGER centavos — never float)
    - RLS on transacoes table means no explicit user_id filter needed in getByMonth/update/delete
---

<objective>
Create the predefined categories utility and the transaction service layer — the server-state foundation for Phase 3. Making the transacao.service.test.ts tests pass (GREEN) is the acceptance gate.

Purpose: categories.ts centralizes the MEI service-provider category list for reuse in Phase 4 (billing intelligence filters) and Phase 9 (charts). transacao.service.ts is the single Supabase access point for the transacoes table — per D-08 (service layer purity) enforced since Phase 1.

Output: Two new files. After this plan, the data contract (Transacao interface, CRUD methods, category list) is complete and tested. The hook and UI layers (Plans 03–06) can proceed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-controle-financeiro-core/03-CONTEXT.md
@.planning/phases/03-controle-financeiro-core/03-RESEARCH.md
@.planning/phases/03-controle-financeiro-core/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/utils/categories.ts — predefined MEI category lists</name>
  <files>src/utils/categories.ts</files>
  <read_first>
    - src/utils/currency.ts — Pattern to replicate: no imports, JSDoc comment at top explaining purpose, named exports only, pure TypeScript constants.
    - .planning/phases/03-controle-financeiro-core/03-RESEARCH.md section "Q10: Categorias Predefinidas" — exact list of strings to use.
    - .planning/phases/03-controle-financeiro-core/03-PATTERNS.md section "src/utils/categories.ts (utility, transform)" — named export shape.
  </read_first>
  <behavior>
    - CATEGORIAS_ENTRADA is a readonly tuple of exactly: 'Servicos Prestados', 'Venda de Produtos', 'Outros' (per D-09)
    - CATEGORIAS_SAIDA is a readonly tuple of exactly: 'Materiais e Suprimentos', 'Transporte', 'Alimentacao', 'Software e Assinaturas', 'Impostos e DAS', 'Marketing e Publicidade', 'Equipamentos', 'Outros'
    - TODAS_CATEGORIAS is the spread union of both arrays
    - Categoria type is typeof TODAS_CATEGORIAS[number]
  </behavior>
  <action>
    Create src/utils/categories.ts. Follow the exact pattern of currency.ts: JSDoc comment block at top explaining purpose (D-09: lista predefinida de categorias para MEI prestador de servicos — habilita filtros coerentes nas Fases 4 e 9), then named exports using "as const" for type-narrowing.

    Export four things in this order:
    1. CATEGORIAS_ENTRADA — string tuple as const per D-09 decision, using the exact strings from RESEARCH.md Q10 (Servicos Prestados, Venda de Produtos, Outros).
    2. CATEGORIAS_SAIDA — string tuple as const per D-09 decision, using the exact 8 strings from RESEARCH.md Q10.
    3. TODAS_CATEGORIAS — spread of both arrays as const.
    4. Categoria — type alias from typeof TODAS_CATEGORIAS[number].

    No imports needed — pure constants. This file intentionally has no test file (pure data, no logic to test).
  </action>
  <verify>
    <automated>npx tsc --noEmit src/utils/categories.ts 2>&1 && node -e "const c=require('./src/utils/categories.ts');console.log('ok')" 2>&1 || npx vitest run --reporter=verbose 2>&1 | grep -E "categories|PASS|FAIL" | head -5</automated>
  </verify>
  <done>
    - src/utils/categories.ts exists.
    - File exports CATEGORIAS_ENTRADA (3 items), CATEGORIAS_SAIDA (8 items), TODAS_CATEGORIAS, and type Categoria.
    - `npx tsc --noEmit` passes with no new type errors.
    - First item of CATEGORIAS_ENTRADA is 'Servicos Prestados'.
    - First item of CATEGORIAS_SAIDA is 'Materiais e Suprimentos'.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/services/transacao.service.ts — Supabase CRUD service (D-08, T-02, T-03, T-05)</name>
  <files>src/services/transacao.service.ts, src/services/transacao.service.test.ts</files>
  <read_first>
    - src/services/empresa.service.ts — The exact service pattern to mirror: single import from @/lib/supabase, exported interface, exported type aliases (Omit), exported const object with async arrow function methods, "if (error) throw error" error handling — NEVER try/catch in service layer.
    - src/services/empresa.service.test.ts — The Supabase mock chain pattern to replicate: vi.mock('@/lib/supabase') before all imports, buildChain() helper, beforeEach clearAllMocks. Extend buildChain to include insert, update, delete, eq, gte, lte, order methods.
    - supabase/migrations/0001_initial_schema.sql — Verify transacoes table columns: id, user_id, tipo, valor (INTEGER), categoria, descricao, tipo_pessoa, data. Confirm RLS policy uses (select auth.uid()) = user_id.
    - src/services/transacao.service.test.ts — The failing test stubs created in Plan 01 — this task makes them GREEN.
  </read_first>
  <behavior>
    SECURITY VALIDATIONS (required before any Supabase call — mitigates T-02, T-03, T-05):
    - create/update: if cents <= 0 or cents > 999_999_999, throw Error('Valor invalido')
    - create: if tipo not in ['entrada', 'saida'], throw Error('Tipo invalido')
    - create: if tipo_pessoa not in ['PF', 'PJ', null, undefined], throw Error('Tipo de pessoa invalido')
    - create: if data does not match /^\d{4}-\d{2}-\d{2}$/, throw Error('Data invalida')

    SERVICE METHODS:
    - getByMonth(year, month): queries transacoes with .gte('data', from).lte('data', to).order('data', {ascending: false}).order('created_at', {ascending: false}), returns Transacao[] (empty array if data is null)
    - create(input): validates then inserts via .insert(input).select().single(), returns Transacao
    - update(id, patch): validates valor if patch.valor is present, then .update(patch).eq('id', id).select().single(), returns Transacao
    - delete(id): .delete().eq('id', id), returns void
    - RLS handles user isolation — no user_id filter in queries (per security analysis T-01)
  </behavior>
  <action>
    Create src/services/transacao.service.ts following empresa.service.ts structure exactly. The file starts with `import { supabase } from '@/lib/supabase'`.

    Define Transacao interface with fields: id (string), user_id (string), tipo ('entrada' | 'saida'), valor (number — INTEGER centavos, per D-09), categoria (string | null), descricao (string | null), tipo_pessoa ('PF' | 'PJ' | null), data (string — 'YYYY-MM-DD'), created_at (string).

    Define CreateTransacaoInput as Omit<Transacao, 'id' | 'user_id' | 'created_at'>.
    Define UpdateTransacaoInput as Partial<CreateTransacaoInput>.

    Define and export transacaoService as a const object with 4 async methods:
    - getByMonth(year, month): Build from and to date strings using padStart. Call supabase.from('transacoes').select('*').gte('data', from).lte('data', to).order('data', {ascending: false}).order('created_at', {ascending: false}). Return data ?? [].
    - create(input): FIRST validate cents (input.valor > 0 && input.valor <= 999_999_999), tipo, tipo_pessoa, data format. Then insert. The valor must be an integer — document this in JSDoc.
    - update(id, patch): If patch.valor is defined, validate range. Then update and return.
    - delete(id): Delete and return void. No return value.

    All methods follow the pattern: `const { data, error } = await supabase...` then `if (error) throw error` then `return data`.

    After creating the service file, update src/services/transacao.service.test.ts to make all tests GREEN. The test file was created as RED stubs in Plan 01 — now add real assertions that verify the exact Supabase call chain (which table, which methods, in which order). Extend buildChain() to include the additional chain methods needed by transacaoService (insert, update, delete, eq, gte, lte, order). Verify `npx vitest run src/services/transacao.service.test.ts` exits 0.
  </action>
  <verify>
    <automated>npx vitest run src/services/transacao.service.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - src/services/transacao.service.ts exists, exports Transacao interface, CreateTransacaoInput, UpdateTransacaoInput, transacaoService.
    - transacaoService has methods: getByMonth, create, update, delete.
    - create() throws 'Valor invalido' when valor <= 0 or valor > 999_999_999 (T-02).
    - create() throws 'Tipo invalido' when tipo is not 'entrada' or 'saida' (T-03).
    - create() throws 'Tipo de pessoa invalido' when tipo_pessoa is not 'PF', 'PJ', or null/undefined (T-03).
    - create() throws 'Data invalida' when data does not match YYYY-MM-DD pattern (T-05).
    - `npx vitest run src/services/transacao.service.test.ts` exits 0 (GREEN).
    - `npx tsc --noEmit` passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Supabase RLS | transacaoService calls cross this boundary; RLS enforces user isolation server-side |
| User input → service layer | CreateTransacaoInput fields arrive from TransactionSheet; service validates before insert |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | transacaoService.getByMonth | high | mitigate | RLS `using ((select auth.uid()) = user_id)` on transacoes table enforces isolation server-side. Service does NOT add explicit user_id filter — RLS handles it. Verified in 0001_initial_schema.sql. |
| T-03-02 | Tampering | transacaoService.create / .update | high | mitigate | Validate cents > 0 && cents <= 999_999_999 before insert/update — throws 'Valor invalido' per D-06 security requirement. |
| T-03-03 | Tampering | transacaoService.create | medium | mitigate | Validate tipo in ['entrada', 'saida'] and tipo_pessoa in ['PF', 'PJ', null] before insert — throws typed errors. |
| T-03-04 | Tampering | TransactionSheet (future Plan 05) | low | mitigate | descricao rendered as JSX text node — dangerouslySetInnerHTML must never be used for transaction fields (T-04). Noted here for Plan 05 implementer. |
| T-03-05 | Tampering | transacaoService.create | medium | mitigate | Validate data matches /^\d{4}-\d{2}-\d{2}$/ before insert — throws 'Data invalida'. |
</threat_model>

<verification>
Wave 1a (Plan 02) complete when:
- `npx vitest run src/services/transacao.service.test.ts` exits 0
- `npx tsc --noEmit` passes with no new type errors
- src/utils/categories.ts exports 4 items: CATEGORIAS_ENTRADA (3), CATEGORIAS_SAIDA (8), TODAS_CATEGORIAS (11), Categoria
- src/services/transacao.service.ts exports Transacao, CreateTransacaoInput, UpdateTransacaoInput, transacaoService
</verification>

<success_criteria>
Plan 02 complete when:
1. src/utils/categories.ts has correct entry lists — CATEGORIAS_ENTRADA 3 items, CATEGORIAS_SAIDA 8 items.
2. src/services/transacao.service.ts has all 4 CRUD methods with security validation for T-02, T-03, T-05.
3. `npx vitest run src/services/transacao.service.test.ts` exits 0 — all service tests GREEN.
4. No @radix-ui imports anywhere in new files.
5. No try/catch in service layer — only "if (error) throw error" pattern.
</success_criteria>

## Artifacts This Phase Produces

From this plan:
- `src/utils/categories.ts` — CATEGORIAS_ENTRADA, CATEGORIAS_SAIDA, TODAS_CATEGORIAS, type Categoria
- `src/services/transacao.service.ts` — interface Transacao, type CreateTransacaoInput, type UpdateTransacaoInput, const transacaoService { getByMonth, create, update, delete }

<output>
Create `.planning/phases/03-controle-financeiro-core/03-02-SUMMARY.md` when done.
</output>
