---
phase: 03-controle-financeiro-core
plan: 04
type: execute
wave: 2
depends_on:
  - 03-02
  - 03-03
files_modified:
  - src/hooks/useTransacoes.ts
  - src/hooks/useTransacoesSummary.ts
  - src/hooks/useTransacoesSummary.test.ts
autonomous: true
requirements:
  - FIN-01
  - FIN-05
must_haves:
  truths:
    - useTransacoes(year, month) returns TanStack Query result with queryKey ['transacoes', year, month] and staleTime 0 (D-20)
    - useTransacoes uses retry false — same as other project hooks
    - useTransacoesSummary(year, month) calls useTransacoes internally — no separate Supabase query
    - useTransacoesSummary.summary.entradas sums only tipo='entrada' transactions (centavos)
    - useTransacoesSummary.summary.saidas sums only tipo='saida' transactions (centavos)
    - useTransacoesSummary.summary.saldo = entradas - saidas (can be negative)
    - useTransacoesSummary.summary.lucro is an alias of saldo (same value)
    - "npx vitest run src/hooks/useTransacoesSummary.test.ts" exits 0 (GREEN)
    - TanStack Query deduplicates: two components calling useTransacoes(2026, 6) trigger only one fetch
  artifacts:
    - src/hooks/useTransacoes.ts
    - src/hooks/useTransacoesSummary.ts
  key_links:
    - useTransacoes imports transacaoService from '@/services/transacao.service' (Plan 02)
    - useTransacoesSummary imports useTransacoes from './useTransacoes' — no direct Supabase call
    - queryKey ['transacoes', year, month] — namespace 'transacoes' matches invalidateQueries(['transacoes']) in D-21
---

<objective>
Create the two TanStack Query hooks that power Phase 3's data display. This is the first time the vertical slice becomes visible: after Plan 05 (TransactionSheet), users can record transactions that are fetched by these hooks and displayed by Plans 06 (InicioTab + FinancasTab).

Purpose: useTransacoes wraps the service call with caching per D-20 (staleTime 0 — always fresh). useTransacoesSummary derives the 4 metric values (saldo, entradas, saídas, lucro) from cached data without an extra query. Making useTransacoesSummary.test.ts GREEN proves the business logic is correct before UI builds on it.

Output: Two hook files with GREEN tests. After this plan, both InicioTab and FinancasTab have the data layer they need.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-controle-financeiro-core/03-CONTEXT.md
@.planning/phases/03-controle-financeiro-core/03-RESEARCH.md
@.planning/phases/03-controle-financeiro-core/03-PATTERNS.md
@.planning/phases/03-controle-financeiro-core/03-02-SUMMARY.md
@.planning/phases/03-controle-financeiro-core/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/hooks/useTransacoes.ts — TanStack Query wrapper for transacaoService.getByMonth (D-20)</name>
  <files>src/hooks/useTransacoes.ts</files>
  <read_first>
    - src/hooks/useOnboardingCnpj.ts lines 80-91 — The exact useQuery pattern: import { useQuery } from '@tanstack/react-query', return useQuery<DataType, Error>({ queryKey: [...], queryFn: ..., staleTime: ..., retry: false }). For useTransacoes: staleTime is 0 (D-20), no enabled guard (always fetches), queryKey is ['transacoes', year, month].
    - src/services/transacao.service.ts (created in Plan 02) — transacaoService.getByMonth(year, month) signature and Transacao type.
    - .planning/phases/03-controle-financeiro-core/03-RESEARCH.md section "Q4: useTransacoes hook" — exact differences from useOnboardingCnpj: staleTime 0, no enabled condition, queryKey with year/month params.
  </read_first>
  <action>
    Create src/hooks/useTransacoes.ts.

    Import { useQuery } from '@tanstack/react-query'. Import { transacaoService, type Transacao } from '@/services/transacao.service'.

    Export function useTransacoes(year: number, month: number) returning the result of useQuery<Transacao[], Error>({...}).

    Query configuration:
    - queryKey: ['transacoes', year, month] — matches the 'transacoes' namespace used by invalidateQueries (D-21)
    - queryFn: () => transacaoService.getByMonth(year, month)
    - staleTime: 0 — per D-20, always refetch on mount
    - retry: false — project-wide standard (same as useOnboardingCnpj)

    No other configuration needed. The hook is a thin wrapper — keep it small.

    No test file for useTransacoes directly — the behavior is tested through useTransacoesSummary which mocks useTransacoes. The integration is verified via E2E behavior in Plan 06 acceptance criteria.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <done>
    - src/hooks/useTransacoes.ts exports function useTransacoes(year, month).
    - queryKey is ['transacoes', year, month].
    - staleTime is 0 (the literal number 0, not undefined).
    - retry is false.
    - `npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/hooks/useTransacoesSummary.ts — derived aggregation hook + make summary tests GREEN (D-10, FIN-05)</name>
  <files>src/hooks/useTransacoesSummary.ts, src/hooks/useTransacoesSummary.test.ts</files>
  <read_first>
    - src/hooks/useTransacoes.ts (just created in Task 1) — the hook this wraps.
    - src/hooks/useTransacoesSummary.test.ts — The failing test stubs from Plan 01. This task makes them GREEN.
    - src/utils/currency.ts — centsToBRL is used by consumers (InicioTab) but NOT inside this hook — this hook returns raw centavos numbers.
    - .planning/phases/03-controle-financeiro-core/03-RESEARCH.md section "Q5: useTransacoesSummary hook" — exact reduce pattern for aggregation.
    - .planning/phases/03-controle-financeiro-core/03-PATTERNS.md section "src/hooks/useTransacoesSummary.ts (hook, transform)" — vi.mock('./useTransacoes') pattern for tests.
  </read_first>
  <behavior>
    TransacoesSummary interface:
    - entradas: number — sum of .valor for tipo='entrada' rows (centavos)
    - saidas: number — sum of .valor for tipo='saida' rows (centavos)
    - saldo: number — entradas - saidas (can be negative)
    - lucro: number — alias of saldo (same value — MEI terminology per D-10)

    Aggregation: start with { entradas: 0, saidas: 0, saldo: 0, lucro: 0 }, reduce over transacoes array.
    isLoading and error forwarded from useTransacoes result.

    Test cases (make stubs GREEN):
    - [entrada 500, entrada 300, saida 200] → entradas=800, saidas=200, saldo=600, lucro=600
    - [] → all zeros
    - [saida 500, entrada 200] → entradas=200, saidas=500, saldo=-300, lucro=-300
    - isLoading=true from mock → isLoading=true in result
  </behavior>
  <action>
    Create src/hooks/useTransacoesSummary.ts.

    Import useTransacoes from './useTransacoes'.

    Export interface TransacoesSummary { entradas: number; saidas: number; saldo: number; lucro: number }.

    Export function useTransacoesSummary(year: number, month: number) returning { summary: TransacoesSummary, isLoading: boolean, error: Error | null }.

    Inside: call const { data: transacoes = [], isLoading, error } = useTransacoes(year, month). Then reduce transacoes to compute summary (accumulate entradas when t.tipo === 'entrada', saidas otherwise). After reduce: summary.saldo = summary.entradas - summary.saidas; summary.lucro = summary.saldo. Return { summary, isLoading, error }.

    After creating the hook, update src/hooks/useTransacoesSummary.test.ts to make all tests GREEN. The test file mocks './useTransacoes' (vi.mock) so no TanStack Query provider is needed. Each test calls vi.mocked(useTransacoes).mockReturnValue({ data: [...], isLoading: false, error: null, ...otherQueryFields }) then uses renderHook to get the result. Verify summary values match the expected aggregations per behavior cases above.

    Run `npx vitest run src/hooks/useTransacoesSummary.test.ts` — must exit 0.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useTransacoesSummary.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - src/hooks/useTransacoesSummary.ts exports TransacoesSummary interface and useTransacoesSummary function.
    - saldo = entradas - saidas (negative when saidas > entradas).
    - lucro is a numeric alias of saldo (same value).
    - isLoading forwarded correctly from useTransacoes.
    - `npx vitest run src/hooks/useTransacoesSummary.test.ts` exits 0 (GREEN).
    - `npx tsc --noEmit` passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TanStack Query cache → component render | Cached Transacao[] flows to summary calculation; no user data crosses trust boundary here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | useTransacoes queryFn | high | mitigate | RLS enforced server-side by Supabase — useTransacoes calls transacaoService.getByMonth which has no user_id filter; RLS on transacoes table (`using ((select auth.uid()) = user_id)`) enforces user isolation automatically. |
| T-03-W4-01 | Tampering | useTransacoesSummary | low | accept | Summary calculation is pure arithmetic on server-returned integers — no external input, no injection surface. |
| T-03-04 | Tampering | Hook consumers (InicioTab, FinancasTab) | low | mitigate | summary.entradas/saidas/saldo/lucro are centavos integers passed to centsToBRL() for display — rendered as JSX text, never as HTML. Plan 06 must not use dangerouslySetInnerHTML. |
</threat_model>

<verification>
Wave 2 (Plan 04) complete when:
- `npx vitest run src/hooks/useTransacoesSummary.test.ts` exits 0
- `npx tsc --noEmit` passes
- src/hooks/useTransacoes.ts: queryKey=['transacoes', year, month], staleTime=0, retry=false
- src/hooks/useTransacoesSummary.ts: returns { summary: TransacoesSummary, isLoading, error }
- Combined test run: `npx vitest run src/services/ src/hooks/` — all pass
</verification>

<success_criteria>
Plan 04 complete when:
1. useTransacoes.ts: queryKey includes namespace 'transacoes', staleTime 0, retry false.
2. useTransacoesSummary.ts: correct aggregation — saldo can be negative, lucro === saldo.
3. `npx vitest run src/hooks/useTransacoesSummary.test.ts` exits 0 (GREEN).
4. `npx vitest run src/services/ src/hooks/` runs all tests — all GREEN.
5. `npx tsc --noEmit` passes.
</success_criteria>

## Artifacts This Phase Produces

From this plan:
- `src/hooks/useTransacoes.ts` — function useTransacoes(year: number, month: number): UseQueryResult<Transacao[], Error>
- `src/hooks/useTransacoesSummary.ts` — interface TransacoesSummary, function useTransacoesSummary(year, month): { summary, isLoading, error }

<output>
Create `.planning/phases/03-controle-financeiro-core/03-04-SUMMARY.md` when done.
</output>
