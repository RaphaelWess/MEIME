---
phase: 03-controle-financeiro-core
plan: 03
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - src/hooks/useCurrencyInput.ts
  - src/hooks/useCurrencyInput.test.ts
  - src/stores/financas.store.ts
autonomous: true
requirements:
  - FIN-01
  - FIN-02
  - FIN-05
must_haves:
  truths:
    - useCurrencyInput(0).displayValue contains 'R$' and '0,00' on init (D-07)
    - handleChange with e.target.value='1234' results in cents=1234, displayValue contains '12,34' (D-07 push-right)
    - handleChange strips non-digits — displayValue=R$ 12,34 passed back in yields same cents (iOS backspace safe)
    - handleChange ignores input when parsed cents > 999_999_999 (T-02 guard)
    - useCurrencyInput returns reset() that sets cents back to 0
    - "npx vitest run src/hooks/useCurrencyInput.test.ts" exits 0 (GREEN)
    - financas.store exports useFinancasStore with selectedYear, selectedMonth initialized from new Date()
    - financas.store openSheet(transaction) sets sheetOpen=true and editingTransaction=transaction
    - financas.store closeSheet() sets sheetOpen=false and editingTransaction=null
    - financas.store setSelectedMonth(year, month) updates both selectedYear and selectedMonth
  artifacts:
    - src/hooks/useCurrencyInput.ts
    - src/stores/financas.store.ts
  key_links:
    - useCurrencyInput imports centsToBRL from '@/utils/currency' (already exists — no modification needed)
    - financas.store imports Transacao type from '@/services/transacao.service' (Plan 02, same wave)
    - new Date() is called OUTSIDE create() at module level (Pitfall 6 prevention)
---

<objective>
Create the push-right currency input hook (D-06, D-07, D-08) and the Zustand UI state store (D-16, D-01). Both run in Wave 1 parallel with Plan 02 — they share no files.

Purpose: useCurrencyInput solves the iOS Safari decimal comma bug (Phase 3 critical requirement, ROADMAP success criteria #1). financas.store.ts provides the shared UI state (selected month, sheet open/closed, editing transaction) that AppShell and both tab pages read.

Output: useCurrencyInput.ts with GREEN tests, financas.store.ts typed and initialized. After this plan, the currency input behavior contract and UI state contract are both fulfilled.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-controle-financeiro-core/03-CONTEXT.md
@.planning/phases/03-controle-financeiro-core/03-RESEARCH.md
@.planning/phases/03-controle-financeiro-core/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/hooks/useCurrencyInput.ts — push-right BRL input hook (D-06, D-07, D-08)</name>
  <files>src/hooks/useCurrencyInput.ts, src/hooks/useCurrencyInput.test.ts</files>
  <read_first>
    - src/hooks/useCnpjMask.ts — Exact import and hook shape to mirror: import { useState } from 'react', export function useCnpjMask() returning { raw, masked, handleChange }. For useCurrencyInput: import { useState, useCallback } from 'react', return { cents, displayValue, handleChange, reset, setCents }.
    - src/utils/currency.ts — centsToBRL function signature: centsToBRL(centavos: number): string. This is the ONLY external dependency. Do NOT call BRLtoCents in this hook.
    - src/hooks/useCurrencyInput.test.ts — The failing test stubs written in Plan 01. Make these GREEN.
    - .planning/phases/03-controle-financeiro-core/03-RESEARCH.md section "Q2: useCurrencyInput — push-right centavos" — exact implementation pattern including the iOS backspace handling explanation.
  </read_first>
  <behavior>
    - useCurrencyInput(initialCents=0): starts with cents=initialCents, displayValue=centsToBRL(initialCents)
    - handleChange strips all non-digit characters via .replace(/\D/g, '') — handles iOS Safari backspace naturally
    - handleChange with '' input → setCents(0)
    - handleChange with '1234' → sets cents to parseInt('1234', 10) = 1234
    - handleChange guards: if parsed newCents > 999_999_999, returns early (no state update)
    - reset() → setCents(0)
    - setCents(n) → updates internal cents state directly (for edit mode pre-fill from TransactionSheet)
    - handleChange uses useCallback with [] deps (no re-creation on render)
    - reset uses useCallback with [] deps
    - The displayValue "R$ 12,34" passed back as e.target.value → after /\D/g strip → "1234" → parseInt → 1234 (idempotent, iOS-safe)
  </behavior>
  <action>
    Create src/hooks/useCurrencyInput.ts.

    Import: useState and useCallback from 'react'. Import centsToBRL from '@/utils/currency'.

    Export function useCurrencyInput(initialCents = 0) with signature returning { cents, displayValue, handleChange, reset, setCents }.

    Use a single useState<number>(initialCents) for cents. The displayValue is derived synchronously: centsToBRL(cents).

    handleChange: wrapped in useCallback with [] deps. Takes React.ChangeEvent<HTMLInputElement>. Strip non-digits from e.target.value with .replace(/\D/g, ''). If result is empty string, setCents(0) and return. Parse as parseInt(digits, 10). If result > 999_999_999, return early. Otherwise setCents(newCents).

    reset: useCallback(() => setCents(0), []).

    The returned setCents is the raw state setter — exposed for edit mode initialization in TransactionSheet (pre-fill when transaction prop is set).

    After creating the hook, update src/hooks/useCurrencyInput.test.ts to make all tests GREEN. Use renderHook and act from '@testing-library/react'. Simulate handleChange by creating a mock event: { target: { value: '1234' } } as React.ChangeEvent<HTMLInputElement>. Wrap state-updating calls in act(). Verify cents and displayValue match expected values per behavior cases above.

    Run `npx vitest run src/hooks/useCurrencyInput.test.ts` — must exit 0.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useCurrencyInput.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - src/hooks/useCurrencyInput.ts exports function useCurrencyInput(initialCents?: number).
    - Returns { cents, displayValue, handleChange, reset, setCents }.
    - Input type="text" inputMode="numeric" pattern works — no type="number" anywhere in this file.
    - `npx vitest run src/hooks/useCurrencyInput.test.ts` exits 0 (GREEN).
    - `npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create src/stores/financas.store.ts — UI state for selected month and sheet open/close (D-01, D-05, D-16)</name>
  <files>src/stores/financas.store.ts</files>
  <read_first>
    - src/stores/empresa.store.ts — Exact Zustand store pattern: import { create } from 'zustand', typed interface, export const useXStore = create<Interface>((set) => ({...})). Each setter is an arrow function calling set({}). No async logic.
    - .planning/phases/03-controle-financeiro-core/03-RESEARCH.md section "Q6: Month Navigator — Zustand Store" — exact store shape including the prevMonth/nextMonth utility functions.
    - .planning/phases/03-controle-financeiro-core/03-PATTERNS.md section "src/stores/financas.store.ts (store, event-driven)" — Pitfall 6: new Date() must be called OUTSIDE create() at module level.
    - src/services/transacao.service.ts — Transacao interface (created in Plan 02, same wave) — needed for editingTransaction: Transacao | null type.
  </read_first>
  <action>
    Create src/stores/financas.store.ts.

    Import: { create } from 'zustand'. Import type { Transacao } from '@/services/transacao.service'.

    CRITICAL — Pitfall 6 prevention: Call `const now = new Date()` at module level (outside create()). This runs exactly once at import time. Use now.getFullYear() and now.getMonth() + 1 as initial values.

    Define FinancasStore interface with these typed fields and setters:
    - selectedYear: number — the calendar year for the month navigator
    - selectedMonth: number — 1-12 (not 0-indexed; getMonth() + 1)
    - sheetOpen: boolean — true when TransactionSheet is open
    - editingTransaction: Transacao | null — null = create mode; non-null = edit mode (D-05)
    - setSelectedMonth(year: number, month: number): void — updates both fields together
    - openSheet(transaction?: Transacao): void — sets sheetOpen=true, editingTransaction=transaction ?? null
    - closeSheet(): void — sets sheetOpen=false, editingTransaction=null

    Export useFinancasStore = create<FinancasStore>((set) => ({ ...initial values and setters })).

    Also export two pure utility functions for month navigation (used in FinancasTab, Plan 06):
    - prevMonth(year: number, month: number): { year: number; month: number } — wraps month 1 → December of previous year
    - nextMonth(year: number, month: number): { year: number; month: number } — wraps month 12 → January of next year

    Export constant MONTHS_PT as string[] of 12 Portuguese month names: ['Janeiro', 'Fevereiro', 'Marco', 'Abril', 'Maio', 'Junho', 'Julho', 'Agosto', 'Setembro', 'Outubro', 'Novembro', 'Dezembro']. Usage: MONTHS_PT[month - 1].

    No test file for this store — Zustand stores are integration-tested via the components that use them.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <done>
    - src/stores/financas.store.ts exists, exports useFinancasStore.
    - useFinancasStore has selectedYear, selectedMonth (initialized from current date), sheetOpen (false), editingTransaction (null).
    - useFinancasStore has setSelectedMonth, openSheet, closeSheet setters.
    - Exports prevMonth, nextMonth utility functions and MONTHS_PT array.
    - `npx tsc --noEmit` passes with no new type errors.
    - new Date() is called outside create() — not inside any setter function.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keyboard input → useCurrencyInput | Raw keystrokes arrive via onChange; hook strips non-digits before converting to centavos |
| Zustand store → component tree | editingTransaction state flows from AppShell down to TransactionSheet via props |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Tampering | useCurrencyInput.handleChange | high | mitigate | Guard newCents > 999_999_999 prevents absurd values from entering hook state; service layer also validates (defense in depth). |
| T-03-W3-01 | Tampering | financas.store editingTransaction | low | accept | editingTransaction holds a Transacao from the server (already RLS-filtered); no trust boundary crossed in the store itself. The boundary is at the Supabase call (T-03-01 in Plan 02). |
</threat_model>

<verification>
Wave 1b (Plan 03) complete when:
- `npx vitest run src/hooks/useCurrencyInput.test.ts` exits 0
- `npx tsc --noEmit` passes
- src/hooks/useCurrencyInput.ts exports useCurrencyInput returning { cents, displayValue, handleChange, reset, setCents }
- src/stores/financas.store.ts exports useFinancasStore, prevMonth, nextMonth, MONTHS_PT
- No type="number" in useCurrencyInput.ts (only type="text" inputMode="numeric" in usage docs/comments)
</verification>

<success_criteria>
Plan 03 complete when:
1. useCurrencyInput.ts: push-right behavior works, iOS backspace safe, guard for >999_999_999.
2. All useCurrencyInput tests GREEN (`npx vitest run src/hooks/useCurrencyInput.test.ts` exits 0).
3. financas.store.ts: exports useFinancasStore, prevMonth, nextMonth, MONTHS_PT. Initialized with current date.
4. `npx tsc --noEmit` passes.
</success_criteria>

## Artifacts This Phase Produces

From this plan:
- `src/hooks/useCurrencyInput.ts` — function useCurrencyInput(initialCents?: number): { cents, displayValue, handleChange, reset, setCents }
- `src/stores/financas.store.ts` — useFinancasStore (selectedYear, selectedMonth, sheetOpen, editingTransaction, setSelectedMonth, openSheet, closeSheet), prevMonth, nextMonth, MONTHS_PT

<output>
Create `.planning/phases/03-controle-financeiro-core/03-03-SUMMARY.md` when done.
</output>
