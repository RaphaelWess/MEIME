---
phase: 03-controle-financeiro-core
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/ui/drawer.tsx
  - src/components/ui/skeleton.tsx
  - src/hooks/useCurrencyInput.test.ts
  - src/services/transacao.service.test.ts
  - src/hooks/useTransacoesSummary.test.ts
autonomous: true
requirements:
  - FIN-01
  - FIN-02
  - FIN-05
must_haves:
  truths:
    - "npx shadcn add drawer" produces src/components/ui/drawer.tsx without @radix-ui imports
    - "npx shadcn add skeleton" produces src/components/ui/skeleton.tsx with animate-pulse
    - useCurrencyInput.test.ts exists with RED (failing) tests for push-right behavior
    - transacao.service.test.ts exists with RED (failing) tests for getByMonth, create, update, delete
    - useTransacoesSummary.test.ts exists with RED (failing) tests for entradas/saidas/saldo aggregation
    - "npx vitest run src/hooks/useCurrencyInput.test.ts" exits non-zero (tests fail — RED phase)
    - "npx vitest run src/services/transacao.service.test.ts" exits non-zero (tests fail — RED phase)
    - "npx vitest run src/hooks/useTransacoesSummary.test.ts" exits non-zero (tests fail — RED phase)
  artifacts:
    - src/components/ui/drawer.tsx
    - src/components/ui/skeleton.tsx
    - src/hooks/useCurrencyInput.test.ts
    - src/services/transacao.service.test.ts
    - src/hooks/useTransacoesSummary.test.ts
  key_links:
    - drawer.tsx imports from "@base-ui/react/drawer" (not @radix-ui)
    - transacao.service.test.ts uses vi.mock('@/lib/supabase') pattern from empresa.service.test.ts
    - tests import modules that do not exist yet — that is what makes them RED
---

<objective>
Install the two shadcn/ui components required by later plans, then write failing (RED) test stubs for the three logic units this phase introduces. Wave 0 is blocking — nothing else can start until both tasks complete.

Purpose: Ensure the @base-ui/react Drawer and Skeleton components are available before TransactionSheet is built (Plan 05). Establish TDD red phase for the three behavioral contracts Wave 1/2 plans must satisfy: push-right currency input, transaction service CRUD, and summary aggregation.

Output: drawer.tsx, skeleton.tsx (installed), and three failing test files whose failures are the acceptance gate for Plans 02–04 (Green phase).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-controle-financeiro-core/03-CONTEXT.md
@.planning/phases/03-controle-financeiro-core/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Drawer and Skeleton components</name>
  <files>src/components/ui/drawer.tsx, src/components/ui/skeleton.tsx</files>
  <read_first>
    - src/components/ui/alert-dialog.tsx — The exact wrapping pattern to replicate if CLI fails. Imports from "@base-ui/react/alert-dialog", uses cn() from @/lib/utils, each sub-component is a named function wrapping the primitive. Do NOT import from @radix-ui.
    - components.json — Verify "style": "base-nova" is set. The CLI uses this to choose @base-ui/react primitives.
    - src/components/ui/ (directory) — See what components already exist to avoid overwriting.
  </read_first>
  <action>
    Run in sequence (each command must succeed before the next):
    1. Run `npx shadcn add drawer` — installs src/components/ui/drawer.tsx wrapping @base-ui/react/drawer.
    2. Run `npx shadcn add skeleton` — installs src/components/ui/skeleton.tsx with animate-pulse.

    FALLBACK for drawer if CLI fails (A1 risk from RESEARCH.md): Create src/components/ui/drawer.tsx manually by copying the structure of src/components/ui/alert-dialog.tsx verbatim, replacing every occurrence of "AlertDialog" and "AlertDialogPrimitive" with "Drawer" and "DrawerPrimitive", and changing the import to `import { Drawer as DrawerPrimitive } from "@base-ui/react/drawer"`. The DrawerContent (popup) must use bottom-anchored positioning classes: fixed inset-x-0 bottom-0 z-50 with slide-in-from-bottom animation.

    FALLBACK for skeleton if CLI fails: Create src/components/ui/skeleton.tsx manually as a function Skeleton accepting className and ...props, rendering a div with data-slot="skeleton" and classes "bg-accent animate-pulse rounded-md" merged via cn() from @/lib/utils.

    CRITICAL: Neither file may import from @radix-ui/*. If any import containing "@radix-ui" appears, the install used the wrong style — delete the file and use the manual fallback.

    Exported names drawer.tsx must include: Drawer, DrawerContent, DrawerHeader, DrawerTitle, DrawerDescription, DrawerFooter, DrawerClose. These are the exact names TransactionSheet.tsx (Plan 05) will import.

    Exported names skeleton.tsx must include: Skeleton (default or named export).
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const d=f.readFileSync('src/components/ui/drawer.tsx','utf8');if(d.includes('@radix-ui'))throw new Error('radix import detected');if(!d.includes('DrawerContent'))throw new Error('DrawerContent missing');console.log('drawer.tsx OK');const s=f.readFileSync('src/components/ui/skeleton.tsx','utf8');if(!s.includes('animate-pulse'))throw new Error('animate-pulse missing');console.log('skeleton.tsx OK')"</automated>
  </verify>
  <done>
    - src/components/ui/drawer.tsx exists, exports DrawerContent, DrawerHeader, DrawerTitle, DrawerDescription, DrawerFooter, DrawerClose, contains no @radix-ui imports.
    - src/components/ui/skeleton.tsx exists, contains "animate-pulse".
    - `npx tsc --noEmit` passes with no new errors from these files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing test stubs (TDD Red phase) for Wave 1/2 logic units</name>
  <files>src/hooks/useCurrencyInput.test.ts, src/services/transacao.service.test.ts, src/hooks/useTransacoesSummary.test.ts</files>
  <read_first>
    - src/utils/currency.test.ts — Exact test file structure: import { describe, it, expect } from 'vitest', describe blocks, assertion style (toContain, toEqual). Mirror this style exactly.
    - src/services/empresa.service.test.ts — Supabase mock pattern: vi.mock('@/lib/supabase', ...) BEFORE imports, buildChain() fluent chain builder, beforeEach(() => vi.clearAllMocks()), typed mockSupabase. This exact pattern must be replicated in transacao.service.test.ts.
    - src/hooks/useOnboardingCnpj.test.ts — renderHook + act usage pattern (if it exists; if not, check src/hooks/).
    - .planning/phases/03-controle-financeiro-core/03-RESEARCH.md section "Validation Architecture / Wave 0 Gaps" — the exact test cases required.
  </read_first>
  <behavior>
    useCurrencyInput.test.ts cases:
    - initialCents=0 → displayValue contains "R$" and "0,00"
    - handleChange with e.target.value="1234" → cents === 1234 and displayValue contains "12,34"
    - handleChange with e.target.value="" → cents === 0
    - handleChange with value > 999999999 centavos (e.g. "9999999999") → cents unchanged (guard fires)
    - reset() → cents === 0

    transacao.service.test.ts cases (vi.mock('@/lib/supabase') FIRST):
    - getByMonth(2026, 6) calls supabase.from('transacoes').select('*').gte('data', '2026-06-01').lte('data', '2026-06-30') and returns array
    - getByMonth returns [] when supabase returns null data
    - getByMonth throws when supabase returns error
    - create(input) calls supabase.from('transacoes').insert(input).select().single() and returns row
    - create with tipo_pessoa='PF' is accepted (no type error)
    - create with tipo_pessoa='PJ' is accepted (no type error)
    - update(id, patch) calls supabase.from('transacoes').update(patch).eq('id', id).select().single()
    - delete(id) calls supabase.from('transacoes').delete().eq('id', id)

    useTransacoesSummary.test.ts cases (vi.mock('./useTransacoes') to avoid TanStack Query provider):
    - with 2 entradas (500, 300) and 1 saida (200): entradas=800, saidas=200, saldo=600, lucro=600
    - with no transactions: entradas=0, saidas=0, saldo=0
    - with saidas > entradas: saldo is negative
    - isLoading is forwarded from useTransacoes mock
  </behavior>
  <action>
    Create three test files. These files import modules that do NOT exist yet — that is intentional and correct for TDD Red phase.

    useCurrencyInput.test.ts:
    Import { describe, it, expect } from 'vitest', import { renderHook, act } from '@testing-library/react'. Import useCurrencyInput from './useCurrencyInput'. Write describe('useCurrencyInput') with the behavior cases above. The test must fail because useCurrencyInput.ts does not exist yet.

    transacao.service.test.ts:
    CRITICAL: vi.mock('@/lib/supabase', ...) call MUST appear BEFORE any import from the service (same as empresa.service.test.ts pattern). Mirror the buildChain helper from empresa.service.test.ts but extend it to include: insert, update, delete, eq, gte, lte, order, single methods all returning the chain (fluent builder). Write describe blocks for transacaoService.getByMonth, .create, .update, .delete per the behavior cases above.

    useTransacoesSummary.test.ts:
    Import { describe, it, expect, vi } from 'vitest'. Use vi.mock('./useTransacoes') to mock the useTransacoes hook with controlled return values — this avoids needing a TanStack Query provider wrapper. Import { renderHook } from '@testing-library/react'. Write describe('useTransacoesSummary') with the aggregation behavior cases above.

    Do NOT implement the modules — only write the test files. Verify they fail (RED) before finishing this task.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useCurrencyInput.test.ts src/services/transacao.service.test.ts src/hooks/useTransacoesSummary.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All three test files exist and contain the describe/it blocks specified in the behavior cases.
    - Running the tests exits with non-zero (failures), proving RED phase — the tested modules do not exist yet.
    - No TypeScript syntax errors in the test files themselves (only import-resolution errors for missing modules).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn CLI → file system | npx shadcn add writes files; must verify output has no @radix-ui imports |
| Test process → module resolver | Test files import non-existent modules; must confirm vitest reports FAIL not ERROR |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-W0-01 | Tampering | drawer.tsx | low | mitigate | Verify drawer.tsx contains no @radix-ui imports after install; if found, replace with manual fallback using @base-ui/react/drawer |
| T-03-W0-02 | Tampering | shadcn CLI install | low | accept | CLI is already in package.json at 4.12.0 — no new package install, no supply-chain risk |
</threat_model>

<verification>
Wave 0 gates that must be green before any Wave 1 plan starts:
- `node -e "require('fs').readFileSync('src/components/ui/drawer.tsx','utf8')"` exits 0
- `node -e "require('fs').readFileSync('src/components/ui/skeleton.tsx','utf8')"` exits 0
- `npx vitest run src/hooks/useCurrencyInput.test.ts` exits non-zero (RED — missing module)
- `npx vitest run src/services/transacao.service.test.ts` exits non-zero (RED — missing module)
- `npx vitest run src/hooks/useTransacoesSummary.test.ts` exits non-zero (RED — missing module)
- `npx tsc --noEmit` passes (test files have correct TypeScript syntax)
</verification>

<success_criteria>
Wave 0 complete when:
1. drawer.tsx installed — exports Drawer, DrawerContent, DrawerHeader, DrawerTitle, DrawerDescription, DrawerFooter, DrawerClose — no @radix-ui imports
2. skeleton.tsx installed — exports Skeleton with animate-pulse
3. Three test stubs written and failing (RED phase confirmed by non-zero vitest exit)
4. npm run build or npx tsc --noEmit reports no syntax errors in the new files
</success_criteria>

## Artifacts This Phase Produces

From this plan:
- `src/components/ui/drawer.tsx` — Drawer, DrawerContent, DrawerHeader, DrawerTitle, DrawerDescription, DrawerFooter, DrawerClose
- `src/components/ui/skeleton.tsx` — Skeleton
- `src/hooks/useCurrencyInput.test.ts` — test stubs for useCurrencyInput hook
- `src/services/transacao.service.test.ts` — test stubs for transacaoService CRUD
- `src/hooks/useTransacoesSummary.test.ts` — test stubs for useTransacoesSummary aggregation

<output>
Create `.planning/phases/03-controle-financeiro-core/03-01-SUMMARY.md` when done.
</output>
