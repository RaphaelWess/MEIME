---
phase: 02-onboarding-mei
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: false
files_modified:
  - supabase/migrations/0002_empresa_mei_columns.sql
requirements:
  - ONB-02
  - ONB-03
must_haves:
  truths:
    - empresa_mei table has columns cnae_fiscal_descricao, situacao_cadastral (text), data_inicio_atividade (date), atividade_principal (text), data_abertura_mei (date)
    - UNIQUE constraint on empresa_mei.user_id exists so upsert onConflict works correctly
  artifacts:
    - supabase/migrations/0002_empresa_mei_columns.sql
  key_links:
    - 0002 migration applied to live Supabase project before any Wave 2 service code runs
user_setup:
  - service: supabase
    why: "Apply migration 0002 via SQL Editor — Supabase CLI cannot run against the free-tier hosted project without service_role key config"
    dashboard_config:
      - task: "Open SQL Editor, paste and run the contents of supabase/migrations/0002_empresa_mei_columns.sql"
        location: "Supabase Dashboard → project → SQL Editor → New query"
---

<objective>
Add 5 missing columns to empresa_mei and a UNIQUE constraint on user_id.

Purpose: The current schema (migration 0001) is missing columns required by the CONTEXT.md spec (D-07, D-08). Without these columns, empresa.service.ts cannot save the full onboarding payload. Without the UNIQUE constraint, .upsert({ onConflict: 'user_id' }) silently falls back to INSERT behavior, creating duplicate rows.

Output: supabase/migrations/0002_empresa_mei_columns.sql ready to be pasted into Supabase SQL Editor; human checkpoint confirms successful execution.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/migrations/0001_initial_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 0002_empresa_mei_columns.sql</name>
  <files>supabase/migrations/0002_empresa_mei_columns.sql</files>
  <action>
    Create the file with the following SQL (exactly as specified in RESEARCH.md "Schema Gap" section):

    - ALTER TABLE public.empresa_mei ADD COLUMN IF NOT EXISTS cnae_fiscal_descricao text
    - ALTER TABLE public.empresa_mei ADD COLUMN IF NOT EXISTS situacao_cadastral text  (store as TEXT not integer — per RESEARCH.md Pitfall 5 and anti-pattern note: descricao_situacao_cadastral string "ATIVA" is the safe check, not the integer 2)
    - ALTER TABLE public.empresa_mei ADD COLUMN IF NOT EXISTS data_inicio_atividade date
    - ALTER TABLE public.empresa_mei ADD COLUMN IF NOT EXISTS atividade_principal text  (user-editable field, per D-07 step 3)
    - ALTER TABLE public.empresa_mei ADD COLUMN IF NOT EXISTS data_abertura_mei date  (user input for proportional limit, per D-07 step 4 and ONB-03)
    - ALTER TABLE public.empresa_mei ADD CONSTRAINT empresa_mei_user_id_unique UNIQUE (user_id)  (required for upsert onConflict: 'user_id' — per RESEARCH.md Pitfall 2)

    Use ADD COLUMN IF NOT EXISTS for all columns so the migration is safe to run twice without error.
    Use ADD CONSTRAINT with the name empresa_mei_user_id_unique.

    Note: the existing schema (0001) already has cnpj (text), razao_social (text), nome_fantasia (text), cnae_fiscal (integer), data_abertura (date), is_caminhoneiro (boolean). Do NOT drop or rename existing columns.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const sql=fs.readFileSync('supabase/migrations/0002_empresa_mei_columns.sql','utf8'); const required=['cnae_fiscal_descricao','situacao_cadastral','data_inicio_atividade','atividade_principal','data_abertura_mei','empresa_mei_user_id_unique']; const missing=required.filter(c=>!sql.includes(c)); if(missing.length) { console.error('MISSING:',missing); process.exit(1); } console.log('OK — all required identifiers present');"</automated>
  </verify>
  <done>File exists at supabase/migrations/0002_empresa_mei_columns.sql; node verification script exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Migration file 0002_empresa_mei_columns.sql written to disk — adds 5 columns and UNIQUE constraint to empresa_mei.</what-built>
  <how-to-verify>
    1. Open Supabase Dashboard → your project → SQL Editor → New query
    2. Open the file supabase/migrations/0002_empresa_mei_columns.sql and copy its full contents
    3. Paste into the SQL Editor and click "Run"
    4. Verify: no error messages appear
    5. Confirm columns exist: run SELECT column_name FROM information_schema.columns WHERE table_name = 'empresa_mei' ORDER BY column_name; — verify cnae_fiscal_descricao, situacao_cadastral, data_inicio_atividade, atividade_principal, data_abertura_mei appear
    6. Confirm constraint: run SELECT constraint_name FROM information_schema.table_constraints WHERE table_name = 'empresa_mei' AND constraint_type = 'UNIQUE'; — verify empresa_mei_user_id_unique appears
  </how-to-verify>
  <resume-signal>Type "migration applied" when all 5 columns and the UNIQUE constraint are confirmed in Supabase, or describe any error encountered.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→Supabase SQL Editor | DDL statements executed with project owner credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | empresa_mei schema | low | accept | Migration uses ADD COLUMN IF NOT EXISTS — idempotent; cannot corrupt existing data |
| T-02-02 | Tampering | UNIQUE constraint | low | accept | Constraint blocks duplicate user_id rows; safe DDL operation with no data loss risk |
</threat_model>

<verification>
After human checkpoint confirms migration applied:
- supabase/migrations/0002_empresa_mei_columns.sql exists in the repository
- Supabase Table Editor shows the 5 new columns in empresa_mei
- UNIQUE constraint empresa_mei_user_id_unique visible in Table Editor → empresa_mei → Constraints
</verification>

<success_criteria>
Migration file committed to repo. Human confirms all 5 columns and UNIQUE constraint exist in the live Supabase project. Wave 2 can now proceed.
</success_criteria>

<output>
Create .planning/phases/02-onboarding-mei/02-01-SUMMARY.md when done
</output>
