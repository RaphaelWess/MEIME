---
phase: 04-inteligencia-de-faturamento
plan: 05
type: execute
wave: 4
depends_on:
  - 04-04
files_modified:
  - src/pages/InicioTab.tsx
autonomous: false
requirements:
  - FAT-01
  - FAT-02
  - FAT-03
must_haves:
  truths:
    - "FaturamentoGauge appears above the 4-card metrics grid in InicioTab (D-01)"
    - "FaturamentoAlert appears between gauge and 4-card grid, only when alertaAtivo is not null (D-09)"
    - "4 existing metric cards (Saldo, Entradas, Saídas, Lucro) are preserved unchanged below the gauge (D-03)"
    - "fatLoading and isLoading are independent — gauge and monthly cards have separate loading states (RESEARCH.md Pitfall 6)"
    - "empresa null → fatLoading=true → FaturamentoGauge renders skeleton (never R$0/0%)"
    - "FaturamentoGauge receives isCaminhoneiro={empresa?.is_caminhoneiro ?? false}"
  artifacts:
    - src/pages/InicioTab.tsx (modified)
  key_links:
    - "useFaturamento(selectedYear) called with selectedYear from useFinancasStore — selectedYear is the annual gauge year"
    - "alertaAtivo !== null conditional renders FaturamentoAlert between gauge and card grid"
    - "InicioTab is the only component that calls useFaturamento — components are props-only"
---

<objective>
Wave 3: Integrate FaturamentoGauge and FaturamentoAlert into InicioTab above the existing 4 metric cards. Add UAT human checkpoint.

Purpose: This is the final integration step that delivers the core value to users. InicioTab now shows the annual billing intelligence (gauge + alert) above the monthly cash flow context (4 cards).

Output: Modified src/pages/InicioTab.tsx with gauge integration + human UAT verification that the UI works correctly in the running app.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-inteligencia-de-faturamento/04-CONTEXT.md
@.planning/phases/04-inteligencia-de-faturamento/04-RESEARCH.md
@.planning/phases/04-inteligencia-de-faturamento/04-PATTERNS.md
@.planning/phases/04-inteligencia-de-faturamento/04-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate FaturamentoGauge and FaturamentoAlert into InicioTab</name>
  <files>src/pages/InicioTab.tsx</files>
  <read_first>
    - src/pages/InicioTab.tsx — current file: all existing imports, hook calls, and JSX structure (lines 1–49); this is the exact base to modify
    - src/components/FaturamentoGauge.tsx — FaturamentoGaugeProps interface to confirm prop names
    - src/components/FaturamentoAlert.tsx — FaturamentoAlertProps interface to confirm prop names
    - src/hooks/useFaturamento.ts — useFaturamento return type (isLoading, totalFaturado, limiteAnual, percentual, projecao, alertaAtivo, empresa)
    - .planning/phases/04-inteligencia-de-faturamento/04-PATTERNS.md — "InicioTab.tsx — modify" section for exact additions
    - .planning/phases/04-inteligencia-de-faturamento/04-UI-SPEC.md — layout spec (InicioTab composition, gap-6 between sections)
    - .planning/phases/04-inteligencia-de-faturamento/04-CONTEXT.md — D-01, D-03 (layout hierarchy is locked)
  </read_first>
  <behavior>
    - InicioTab renders FaturamentoGauge above the 4-card grid (D-01)
    - InicioTab renders FaturamentoAlert between gauge and cards only when alertaAtivo !== null
    - 4 existing cards (Saldo, Entradas, Saídas, Lucro) remain unchanged and below the gauge
    - fatLoading is independent from isLoading (monthly cards loading state unchanged)
    - FaturamentoGauge receives isLoading={fatLoading} — shows skeleton until useFaturamento resolves
    - FaturamentoGauge receives isCaminhoneiro={empresa?.is_caminhoneiro ?? false}
  </behavior>
  <action>
Modify src/pages/InicioTab.tsx. Read the current file first — the existing structure is the exact base.

Add to existing imports (after existing 4 imports):
- useFaturamento from '@/hooks/useFaturamento'
- FaturamentoGauge from '@/components/FaturamentoGauge' (default import)
- FaturamentoAlert from '@/components/FaturamentoAlert' (default import)

Inside the component function body, after the existing useTransacoesSummary call (line 14), add a second hook call:
const { isLoading: fatLoading, totalFaturado, limiteAnual, percentual, projecao, alertaAtivo, empresa } = useFaturamento(selectedYear)

Modify the return JSX:
- Change the outer div className from "mx-auto max-w-md px-4 py-8" to "mx-auto max-w-md px-4 py-8 flex flex-col gap-6"
- Remove the mb-6 from the h1 (gap-6 on the flex container handles spacing)
- After the h1, add FaturamentoGauge before the existing conditional grid block:

Render FaturamentoGauge with all required props:
- totalFaturado={totalFaturado}
- limiteAnual={limiteAnual}
- percentual={percentual}
- projecao={projecao}
- isCaminhoneiro={empresa?.is_caminhoneiro ?? false}
- isLoading={fatLoading}

After FaturamentoGauge, render the conditional FaturamentoAlert:
{alertaAtivo !== null && (FaturamentoAlert with nivel={alertaAtivo} and limiteAnual={limiteAnual})}

The existing conditional grid block (isLoading ? skeletons : cards) stays exactly as-is below the alert, unchanged. Do NOT modify the 4-card grid layout, class names, or content (D-03).

The final layout order (per D-01, gap-6 flex column):
1. h1 "Início"
2. FaturamentoGauge (always rendered, shows skeleton internally if fatLoading)
3. FaturamentoAlert (conditional — only when alertaAtivo !== null)
4. 2-col grid: 4 metric cards (controlled by isLoading from useTransacoesSummary — unchanged)
  </action>
  <verify>
    <automated>cd "C:/Users/rapha/OneDrive/Documentos/Controle MEIME" && npx tsc --noEmit 2>&1 | grep -i "InicioTab" | head -10</automated>
  </verify>
  <done>
    - src/pages/InicioTab.tsx imports and renders FaturamentoGauge and FaturamentoAlert
    - FaturamentoGauge is positioned above the 4-card grid
    - FaturamentoAlert is conditionally rendered between gauge and cards
    - 4 existing cards are unchanged
    - TypeScript: npx tsc --noEmit produces no errors for InicioTab.tsx
    - npx vitest run exits 0 (no regressions in existing tests)
  </done>
  <acceptance_criteria>
    - InicioTab.tsx imports useFaturamento: grep -c "useFaturamento" src/pages/InicioTab.tsx is at least 2 (import + call)
    - InicioTab.tsx imports FaturamentoGauge: grep -c "FaturamentoGauge" src/pages/InicioTab.tsx is at least 2
    - InicioTab.tsx imports FaturamentoAlert: grep -c "FaturamentoAlert" src/pages/InicioTab.tsx is at least 2
    - alertaAtivo conditional: grep -c "alertaAtivo" src/pages/InicioTab.tsx is at least 1
    - Independent loading states: grep -c "fatLoading" src/pages/InicioTab.tsx is at least 1
    - npx tsc --noEmit produces no errors referencing InicioTab.tsx
    - npx vitest run exits 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: UAT — FaturamentoGauge e alertas funcionam corretamente no app</name>
  <files>— (no files modified by this checkpoint)</files>
  <action>Human verifies the gauge, projection, and alert cards work correctly in the running app. Run dev server and execute the verification steps below.</action>
  <what-built>
    FaturamentoGauge with linear progress bar, consumed text, projection line, and skeleton loading state has been integrated into InicioTab above the 4 metric cards. FaturamentoAlert renders conditionally when a billing threshold is crossed. useFaturamento hook fetches the annual entrada transactions and computes limit, percentage, projection, and active alert level. All Phase 4 calculation logic (calcLimiteAnual, calcPercentual, calcProjecao, calcAlertaAtivo) is implemented as pure functions and tested.
  </what-built>
  <how-to-verify>
    Run the dev server: npm run dev (or npx vite in the project root). Open http://localhost:5173 and sign in.

    1. GAUGE VISIBILITY: Navigate to the home tab (Início). Confirm FaturamentoGauge appears as a card ABOVE the Saldo/Entradas/Saídas/Lucro grid. It should show "Limite Anual" heading, a progress bar, consumed/total text, and optionally a projection line.

    2. GAUGE VALUES: Confirm the gauge shows the correct limit for your test account:
       - If data_abertura_mei is in 2026: the limit should be proportional (e.g., opened in July 2026 → ~R$ 40.500)
       - If data_abertura_mei is before 2026 or null: the limit should be R$ 81.000

    3. SKELETON STATE: If you have a test account with empresa=null (not yet onboarded), confirm the gauge shows skeleton placeholders and NOT R$0 / 0%.

    4. PROJECTION LINE: If you have 2+ months of entrada transactions, a projection line should appear: "Projeção: você atinge o limite em [Mês]/[Ano]" or "Na projeção atual, você termina o ano dentro do limite."

    5. NO ALERT (expected for most accounts): If your test account has less than 70% of the annual limit consumed, FaturamentoAlert should NOT appear between gauge and monthly cards.

    6. ALERT TEST (optional — if you want to test alerts): Add transactions totaling >= 70% of the annual limit in 2026 (e.g., R$ 56.700+ for standard MEI). Verify the yellow alert card appears between gauge and monthly cards.

    7. 4-CARD GRID PRESERVED: Confirm Saldo, Entradas, Saídas, Lucro cards still appear below gauge, with correct monthly values for the current month (unchanged from Phase 3).

    8. FULL TEST SUITE: Run npm run test -- --run and confirm all tests pass (0 failures).
  </how-to-verify>
  <verify>
    <human-check>All 8 verification steps above pass. User types "aprovado".</human-check>
  </verify>
  <done>User types "aprovado" after verifying gauge, projection, alert, and preserved 4-card grid in the running app.</done>
  <resume-signal>Type "aprovado" if all checks pass, or describe any issues found (e.g., "gauge shows 0% when empresa exists", "projection text is wrong", "alert appeared when it should not have")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| InicioTab → useFaturamento | year param from selectedYear (Zustand) — controlled by user via month nav; validated in getByYear |
| InicioTab → FaturamentoAlert → external link | gov.br URL hardcoded in ALERT_CONFIG; not user-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Information Disclosure | InicioTab dual-query independent loading | low | accept | Two independent queries (useFaturamento + useTransacoesSummary) have independent loading states — each component shows its own skeleton; no cross-contamination of data |
| T-04-09 | Tampering | InicioTab D-03 layout constraint | low | accept | D-03 locks the 4-card grid unchanged below gauge; integration task explicitly preserves all 4 card JSX unchanged; regression risk is low given TypeScript compilation check |
</threat_model>

<verification>
After Task 1 + UAT approval:
- npx tsc --noEmit — no TypeScript errors
- npx vitest run — full suite exits 0
- Human UAT: gauge visible above 4-cards, correct values, projection line correct, alert conditional, external link works
</verification>

<success_criteria>
- InicioTab shows FaturamentoGauge above the 4 metric cards on every load
- FaturamentoAlert appears only when a threshold is crossed (70%, 90%, 100%, or R$97.200)
- 4 existing metric cards preserved and unchanged (D-03)
- All 4 Phase 4 success criteria from ROADMAP.md confirmed by UAT:
  1. FaturamentoGauge shows correct proportional limit for MEI that opened mid-year; MEI Caminhoneiro sees R$251.600
  2. Projection displays absolute month/year with correct formula
  3. Alerts at 70%/90%/100% with correct colors and copy
  4. R$97.200 alert includes "Saiba como se desenquadrar" link to gov.br
- Full Vitest suite: 0 failures
</success_criteria>

<output>
Create .planning/phases/04-inteligencia-de-faturamento/04-05-SUMMARY.md when done.

## Artifacts this phase produces

| Symbol | File | Kind |
|--------|------|------|
| InicioTab (modified) | src/pages/InicioTab.tsx | modified page component |
</output>
