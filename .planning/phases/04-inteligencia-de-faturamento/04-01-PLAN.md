---
phase: 04-inteligencia-de-faturamento
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/utils/faturamento.test.ts
  - src/services/transacao.service.test.ts
  - src/hooks/useFaturamento.test.ts
autonomous: true
requirements:
  - FAT-01
  - FAT-02
  - FAT-03
must_haves:
  truths:
    - "src/utils/faturamento.test.ts exists with failing tests for calcLimiteAnual, calcPercentual, calcProjecao, calcAlertaAtivo"
    - "src/services/transacao.service.test.ts extended with failing getByYear test cases"
    - "src/hooks/useFaturamento.test.ts exists with stub tests that fail"
    - "npx vitest run src/utils/faturamento.test.ts exits non-zero (RED phase confirmed)"
  artifacts:
    - src/utils/faturamento.test.ts
    - src/hooks/useFaturamento.test.ts
  key_links:
    - "faturamento.test.ts imports from '@/utils/faturamento' — module does not exist yet, so tests fail with import error"
    - "useFaturamento.test.ts imports from '@/hooks/useFaturamento' — module does not exist yet"
---

<objective>
Wave 0: Write all failing tests for Phase 4 pure calculation logic, getByYear service method, and useFaturamento hook. Tests MUST be failing (RED) at end of this plan — implementation files do not exist yet.

Purpose: Establish the acceptance gates for Waves 1–2 before writing any implementation. This is the TDD red phase — tests document expected behavior in executable form.

Output: Three test files covering all FAT-01, FAT-02, FAT-03 requirements. Full test suite fails because src/utils/faturamento.ts and src/hooks/useFaturamento.ts do not exist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-inteligencia-de-faturamento/04-CONTEXT.md
@.planning/phases/04-inteligencia-de-faturamento/04-RESEARCH.md
@.planning/phases/04-inteligencia-de-faturamento/04-PATTERNS.md
@.planning/phases/04-inteligencia-de-faturamento/04-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing tests for pure calculation functions (faturamento.test.ts)</name>
  <files>src/utils/faturamento.test.ts</files>
  <read_first>
    - src/hooks/useTransacoesSummary.test.ts — test file structure, Vitest import pattern, fake Transacao shape
    - src/services/transacao.service.test.ts — buildChain pattern and overall test file structure
    - .planning/phases/04-inteligencia-de-faturamento/04-RESEARCH.md — "Validation Architecture" section for all required test cases
    - .planning/phases/04-inteligencia-de-faturamento/04-PATTERNS.md — "faturamento.test.ts" section for fake Transacao shape and injectable today pattern
  </read_first>
  <action>
Create src/utils/faturamento.test.ts. Import from vitest: describe, it, expect. Import from '@/utils/faturamento': calcLimiteAnual, calcTotalFaturado, calcPercentual, calcProjecao, calcAlertaAtivo, LIMITE_MEI_PADRAO, LIMITE_CAMINHONEIRO, THRESHOLD_DESENQUADRAMENTO, ProjecaoResult, AlertaNivel. No vi.mock needed — pure functions have no imports to mock.

Use the minimal fake Transacao shape from PATTERNS.md: { id, user_id, tipo: 'entrada', valor, categoria: null, descricao: null, tipo_pessoa: null, data: 'YYYY-MM-DD', created_at: '' }.

Write these describe blocks with the exact test cases from RESEARCH.md Validation Architecture:

describe('calcLimiteAnual'):
- empresa null → returns LIMITE_MEI_PADRAO (8_100_000)
- data_abertura_mei null → returns LIMITE_MEI_PADRAO
- data_abertura_mei.year < currentYear → returns full LIMITE_MEI_PADRAO (D-06)
- is_caminhoneiro=true, data_abertura_mei null → returns LIMITE_CAMINHONEIRO (25_160_000)
- abriu em Janeiro do currentYear (month=1) → mesesRestantes=12 → limite completo (8_100_000)
- abriu em Julho do currentYear (month=7) → mesesRestantes=6 → Math.round(6/12 × 8_100_000) = 4_050_000

For each test case involving empresa: pass a minimal EmpresaMei object with only the fields that calcLimiteAnual uses: { is_caminhoneiro: boolean, data_abertura_mei: string | null }. Cast with 'as any' if needed to satisfy the full EmpresaMei type.

describe('calcTotalFaturado'):
- empty array → 0
- two entradas (500_000 + 300_000) → 800_000

describe('calcPercentual'):
- totalFaturado=0, limiteAnual=8_100_000 → 0
- totalFaturado=4_050_000, limiteAnual=8_100_000 → 50
- totalFaturado=8_100_000, limiteAnual=8_100_000 → 100
- totalFaturado=9_000_000, limiteAnual=8_100_000 → result > 100 (confirm it can exceed 100)

describe('calcProjecao'):
Use injectable today parameter. Pass new Date('2026-06-15') as today for all cases.
- mesesDecorridos=0 (today = Jan 15, use new Date('2026-01-15')) → tipo='hidden' (D-15)
- mediaFaturamentoMensal=0 (totalFaturado=0, mesesDecorridos=5) → tipo='hidden' (D-15)
- ritmo baixo — totalFaturado=100_000, limiteAnual=8_100_000, today=new Date('2026-06-15') (5 complete months): mediaPerMonth=20_000, limiteRestante=8_000_000, meses=400 → target year > 2026 → tipo='dentro_do_limite' (D-16)
- ritmo normal breach this year: totalFaturado=6_000_000, limiteAnual=8_100_000, today=new Date('2026-06-15'): mediaPerMonth=1_200_000, limiteRestante=2_100_000, meses=ceil(2_100_000/1_200_000)=2, target=June+2=August → tipo='mes_ano', mes=8, ano=2026
- already exceeded: totalFaturado=9_000_000, limiteAnual=8_100_000 → tipo='mes_ano', mes=currentMonth, ano=currentYear

describe('calcAlertaAtivo'):
- totalFaturado=9_720_000 (>= THRESHOLD_DESENQUADRAMENTO), limiteAnual=8_100_000 → 97200 (D-10 most severe)
- totalFaturado=8_500_000, percentual=104.9 (< THRESHOLD_DESENQUADRAMENTO, >= 100%) → 100
- totalFaturado=7_400_000, percentual=91.4 → 90
- totalFaturado=5_700_000, percentual=70.4 → 70
- totalFaturado=0, percentual=0 → null

These tests MUST fail because '@/utils/faturamento' module does not exist. The import failure is the expected RED state.

Do NOT write fenced code blocks in this action — this is directive prose only. Read the source files listed in read_first for exact imports and patterns.
  </action>
  <verify>
    <automated>cd "C:/Users/rapha/OneDrive/Documentos/Controle MEIME" && npx vitest run src/utils/faturamento.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
    - src/utils/faturamento.test.ts exists with all describe blocks and test cases listed above
    - Running the test file exits non-zero (import error or test failures = RED phase confirmed)
    - Test count: at least 15 test cases across all describe blocks
  </done>
  <acceptance_criteria>
    - File exists: src/utils/faturamento.test.ts
    - Contains describe blocks: calcLimiteAnual, calcTotalFaturado, calcPercentual, calcProjecao, calcAlertaAtivo
    - npx vitest run src/utils/faturamento.test.ts exits with non-zero code (RED)
    - At least 15 individual it() cases
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: Extend transacao.service.test.ts with getByYear failing cases</name>
  <files>src/services/transacao.service.test.ts</files>
  <read_first>
    - src/services/transacao.service.test.ts — current file: full content, especially buildChain helper (lines 18–46) and buildGetByMonthChain pattern (lines 52–67)
    - .planning/phases/04-inteligencia-de-faturamento/04-PATTERNS.md — "getByYear service test" section for buildGetByYearChain wiring
  </read_first>
  <action>
Append to src/services/transacao.service.test.ts (do not modify existing tests). Add a new describe block: describe('transacaoService.getByYear').

The getByYear query chain is: supabase.from('transacoes').select('*').eq('tipo','entrada').gte('data', from).lte('data', to).order('data', {ascending:true})

Chain wiring (terminal is .order at the end):
- Build innerChain: innerChain.order.mockResolvedValue(resolvedValue) — this is the terminal .order()
- Build lteChain: lteChain.order.mockReturnValue(innerChain) — lteChain returns innerChain on .order()
- Build gteChain: gteChain.lte.mockReturnValue(lteChain)
- Build eqChain: eqChain.gte.mockReturnValue(gteChain)
- Build selectChain: selectChain.eq.mockReturnValue(eqChain)
- Wire supabase.from to return a chain where .select() returns selectChain

Write this function buildGetByYearChain(resolvedValue) inside the describe block.

Write these test cases:
- "returns array of entrada transactions for the year" — mock returns { data: [{tipo:'entrada', valor:500_000, data:'2026-03-15', id:'u1', user_id:'u1', categoria:null, descricao:null, tipo_pessoa:null, created_at:''}], error: null } → service returns array with length 1
- "returns empty array when no rows" — mock returns { data: null, error: null } → returns []
- "throws when Supabase returns error" — mock returns { data: null, error: new Error('db error') } → expect getByYear to reject
- "throws for year below 2020" — call transacaoService.getByYear(2019) → expect to reject (year validation per RESEARCH.md V5)
- "throws for year above 2050" — call transacaoService.getByYear(2051) → expect to reject

These test cases for getByYear MUST fail because the method does not exist yet (property does not exist on transacaoService → TypeError). That is the expected RED state.
  </action>
  <verify>
    <automated>cd "C:/Users/rapha/OneDrive/Documentos/Controle MEIME" && npx vitest run src/services/transacao.service.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    - New describe('transacaoService.getByYear') block appended to transacao.service.test.ts
    - Existing getByMonth tests still pass (they were already green)
    - getByYear tests fail with TypeError (method not found) — RED state confirmed
  </done>
  <acceptance_criteria>
    - File src/services/transacao.service.test.ts contains describe('transacaoService.getByYear') block
    - 5 new test cases present (returns array, returns empty, throws on error, throws year < 2020, throws year > 2050)
    - npx vitest run src/services/transacao.service.test.ts exits non-zero (getByYear tests RED)
    - Existing getByMonth tests are NOT broken (they remain green in isolation if getByYear is skipped)
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 3: Write failing stub tests for useFaturamento hook</name>
  <files>src/hooks/useFaturamento.test.ts</files>
  <read_first>
    - src/hooks/useTransacoesSummary.test.ts — vi.mock pattern, renderHook usage, mockReturnValue structure
    - .planning/phases/04-inteligencia-de-faturamento/04-PATTERNS.md — "vi.mock + renderHook pattern" section
    - .planning/phases/04-inteligencia-de-faturamento/04-RESEARCH.md — "useFaturamento Hook Pattern" section
  </read_first>
  <action>
Create src/hooks/useFaturamento.test.ts. This file tests the hook integration contract (not the pure functions — those are in faturamento.test.ts).

Import from vitest: describe, it, expect, vi, beforeEach. Import renderHook from '@testing-library/react'.

Set up vi.mock calls BEFORE importing the hook (hoisting requirement):
- vi.mock('@/services/transacao.service', ...) → { transacaoService: { getByYear: vi.fn() } }
- vi.mock('@/stores/empresa.store', ...) → { useEmpresaStore: vi.fn() }
- vi.mock('@tanstack/react-query', ...) → { useQuery: vi.fn() }

Import useFaturamento from './useFaturamento' after the vi.mock calls.

Write describe('useFaturamento') with these test cases:
- "when empresa is null: isLoading is true and query is not enabled" — mock useEmpresaStore to return { empresa: null }, mock useQuery to return { data: [], isLoading: false, error: null }; render hook; expect result.current.isLoading to be true (because empresa === null forces isLoading override)
- "queryKey starts with 'transacoes'" — mock useEmpresaStore to return { empresa: { is_caminhoneiro: false, data_abertura_mei: null } as any }; capture the useQuery call args; expect the first element of queryKey to equal 'transacoes'
- "returns limiteAnual = LIMITE_MEI_PADRAO for standard MEI with null data_abertura_mei" — mock empresa with is_caminhoneiro=false, data_abertura_mei=null; mock useQuery to return { data: [], isLoading: false, error: null }; expect result.current.limiteAnual to be 8_100_000

These tests MUST fail because '@/hooks/useFaturamento' does not exist. Import error = RED state.

Add beforeEach(() => vi.clearAllMocks()) to prevent test pollution.
  </action>
  <verify>
    <automated>cd "C:/Users/rapha/OneDrive/Documentos/Controle MEIME" && npx vitest run src/hooks/useFaturamento.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
    - src/hooks/useFaturamento.test.ts exists with 3 test cases
    - Test file exits non-zero (module not found = RED phase confirmed)
    - Tests document the contractual behaviors: empresa null → isLoading true, queryKey starts with 'transacoes', limiteAnual = 8_100_000 for standard MEI
  </done>
  <acceptance_criteria>
    - File exists: src/hooks/useFaturamento.test.ts
    - vi.mock calls for transacaoService, useEmpresaStore, and useQuery are present before the useFaturamento import
    - 3 test cases present
    - npx vitest run src/hooks/useFaturamento.test.ts exits non-zero (RED — module not found)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → utils | Test imports from non-existent '@/utils/faturamento' — expected RED-phase import failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-W0-01 | Tampering | Test file authorship | low | accept | Test files are developer-authored, not user input; no runtime risk |
</threat_model>

<verification>
Full wave verification: run all three test files after writing. All MUST exit non-zero.
- npx vitest run src/utils/faturamento.test.ts — non-zero (RED)
- npx vitest run src/services/transacao.service.test.ts — non-zero (RED for getByYear cases)
- npx vitest run src/hooks/useFaturamento.test.ts — non-zero (RED)
</verification>

<success_criteria>
- Three test files exist with failing tests covering FAT-01, FAT-02, FAT-03 acceptance gates
- Wave 0 requirements from VALIDATION.md are satisfied
- RED phase confirmed: all new test runs exit non-zero
- Zero modifications to existing passing tests
</success_criteria>

<output>
Create .planning/phases/04-inteligencia-de-faturamento/04-01-SUMMARY.md when done.

## Artifacts this phase produces

| Symbol | File | Kind |
|--------|------|------|
| faturamento.test.ts (failing) | src/utils/faturamento.test.ts | test file |
| transacaoService.getByYear test cases (failing) | src/services/transacao.service.test.ts | test extension |
| useFaturamento.test.ts (failing) | src/hooks/useFaturamento.test.ts | test file |
</output>
